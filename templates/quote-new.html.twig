{% extends 'base.html.twig' %}

{% block main %}
    <h1>New Quote</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-primary" value="Create">
    {{ form_end(form) }}
{% endblock %}
